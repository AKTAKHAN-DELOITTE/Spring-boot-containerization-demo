spring.application.name=spring-boot-containerization

# MongoDB connection settings
# NOTE: Spring Boot 4.x renamed these from "spring.data.mongodb.*" to
# "spring.mongodb.*" (the old prefix is deprecated and no longer bound).
spring.mongodb.host=localhost
spring.mongodb.port=27017
spring.mongodb.database=moviebookingdb

